/** @jsx React.DOM */

var React = require('react');
var Router = require('react-router');

var Map = React.createClass({

  render: function () {
    return (
      <div>
        <h1>MAP</h1>
      </div>
    );
  }
});

module.exports = Map;
